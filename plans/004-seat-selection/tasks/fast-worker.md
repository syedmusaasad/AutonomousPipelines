## Fetch
Fetch the contents of the given file and write to `$OUT/fetch.txt`.
## Rename
Rename the variable in `$OUT/code.py` from `x` to `y`.
## Fix
Fix the typo in the string literal in `$OUT/typo.txt`.
## Formatting
Format the JSON string in `$OUT/data.json` properly.
## Info
Extract the email address from `$OUT/contact.txt`.
