#!/usr/bin/env bash
# Sticky: resolves the estate through ~/.system/estate (written by bootstrap only).
set -euo pipefail
HERE="$(cd "$(dirname "$(readlink -f "$0")")/.." && pwd)"
export PYTHONPATH="$HERE${PYTHONPATH:+:$PYTHONPATH}"
exec python3 -m pipeline.cli "$@"
