# Migration tasks

- [ ] 1. Create `migration/item-1.done` (`test -f migration/item-1.done`).
- [ ] 2. Create `migration/item-2.done` (`test -f migration/item-2.done`).
- [ ] 3. Create `migration/item-3.done` (`test -f migration/item-3.done`).
- [ ] 4. Create `migration/item-4.done` (`test -f migration/item-4.done`).
